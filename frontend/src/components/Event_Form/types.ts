export interface IFormEvent {
    eventName: string;
    currency: string;
    participants: string[];
    createrName: string;
}

export interface UserSuggestion {
    id: string;
    name: string;
    email: string;
}